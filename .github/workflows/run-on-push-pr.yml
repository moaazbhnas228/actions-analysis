name: Should run on push and prs into staging

on:
  push:
    branches:
      - staging
  pull_request:
    branches:
      - staging

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Print my name
        run: echo "Hello, Moaaz Bhnas"
